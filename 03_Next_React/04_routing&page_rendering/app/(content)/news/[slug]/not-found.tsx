export default function NewsNotFoundPage(): JSX.Element {
  return (
    <div id="error">
      <h1>Not Found!</h1>
      <p>Unfortunately, we could not find the requested article.</p>
    </div>
  );
}
